//To be called when dojo's domReady state is fired:
window.onDomReady = new Array();
/*
	function addToStack(stackName, functionName){
		if(typeof(window.stackName) == "undefined"){}
	}

	//Nom des pages a afficher
	window.pageContent=["entete", "menu", "contenu", "pied"];
	//window.stacks=["GEOMETRY"];
	window.procedures=["onWindowResize":["GEOMETRY"]];
*/
